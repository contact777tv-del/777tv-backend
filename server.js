import "dotenv/config"; import express from "express"; import cors from "cors"; const app = express(); app.use(cors()); app.use(express.json()); app.get("/", (req, res) => { res.send("777TV backend running"); }); app.get("/api/youtube/playlist", async (req, res) => { const { playlistId } = req.query; const key = process.env.YOUTUBE_API_KEY; if (!playlistId || !key) { return res.status(400).json({ error: "Missing playlistId or API key" }); } const url = `https://www.googleapis.com/youtube/v3/playlistItems?part=snippet,contentDetails&maxResults=20&playlistId=${playlistId}&key=${key}`; const response = await fetch(url); const data = await response.json(); res.json(data); }); app.listen(3000, () => { console.log("777TV backend running on port 3000"); });